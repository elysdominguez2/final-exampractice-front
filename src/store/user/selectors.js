export const selectToken = (state) => state.user.token;
export const selectUser = (state) => state.user.profile;
export const selectMySpace = (state) => state.user.space; // Agregue esto para traer la info de Redux de mi nuevo espacio
